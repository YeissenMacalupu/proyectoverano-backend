function getCharacters(done) {
    const results = fetch('http://localhost:8080/api/monitores');
    results
        .then(response => response.json())
        .then(data => {
            done(data)
        });
}
getCharacters(data => {
    data.results.forEach(monitores => {
        const article = document.createRange().createContextualFragment( /* HTML*/ `
        <article>
            <div>
                <h1>${monitores.nameProduct}</h1>
                <img src="${nameProduct.imagen}">
                <p>${nameProduct.precio}</p>
            </div> 
        </article>
    `);
        const main = document.querySelector("main");
        main.append(article)
    });
});